name: Auto Commit 

on:
  schedule:
    - cron: '*/5 * * * *' 
  workflow_dispatch:

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    - name: Set Git Identity   # <-- IMPORTANT FIX
      run: |
        git config --global user.email "shahidsayyed0212@gmail.com"
        git config --global user.name "sayyadshahid"

    - name: Generate Random Commits
      run: |
        NUM=20
        echo "Generating exactly 20 commits!"
        for i in $(seq 1 $NUM); do
          echo "$(date)" >> commits-log.txt
          git add commits-log.txt
          git commit -m "Auto commit #$i at $(date)"
        done

    - name: Push Changes
      run: |
        git push
